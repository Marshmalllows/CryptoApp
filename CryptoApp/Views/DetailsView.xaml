<UserControl x:Class="CryptoApp.Views.DetailsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CryptoApp.Views"
      mc:Ignorable="d"
      Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding SelectedViewModel }"/>
        <StackPanel Grid.Row="0" VerticalAlignment="Top" Height="Auto" Orientation="Vertical">
            <TextBox HorizontalAlignment="Center" VerticalAlignment="Top" Text="Crypto details" MaxHeight="100" FontSize="18" BorderThickness="0" />
            <TextBox Margin="10 10 10 0" MaxWidth="" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <ListBox ItemsSource="{Binding FilteredItems}" Margin="10 0 10 0" />
        </StackPanel>
    </Grid>
</UserControl>
