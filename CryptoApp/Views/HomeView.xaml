<UserControl x:Class="CryptoApp.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CryptoApp.Views"
      mc:Ignorable="d"
      Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding SelectedViewModel }"/>
        <TextBox Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" Text="Top 10 cryptos" MaxHeight="100" FontSize="25" BorderThickness="0" />
        <ListBox Grid.Row="1" BorderThickness="0" FontSize="20" ItemsSource="{Binding TopChartsStrings}" />
    </Grid>
</UserControl>
